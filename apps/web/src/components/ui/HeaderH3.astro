---
interface Props {
	/** 見出しの id（aria-labelledby 等に使用） */
	id?: string;
	/** center: Figmaコンポーネント既定 / left: セクション内左寄せ */
	align?: 'center' | 'left';
	class?: string;
}

const { id, class: className = '', align = 'center' } = Astro.props;

const titleClasses =
	'header-h3__title font-bold text-xl md:text-2xl leading-8';
---

<div
	class:list={[
		'header-h3 tracking-wide w-full pb-3',
		align === 'center' ? 'items-center justify-center' : 'items-start',
		className,
	]}
	data-name="header-h3"
>
	<h3
		id={id}
		class:list={[titleClasses, align === 'center' ? 'text-center' : 'text-left']}
	>
		<slot />
	</h3>
</div>
